Not much to say here for now...

dependencies: pygtk, pycairo

run the demo with "python display.py"

TODO: 
      - better, more expressive interface for users to manipulate equation ( move_toward(node, target) is not sufficient)
      - better algorithm for determining which rules to apply
